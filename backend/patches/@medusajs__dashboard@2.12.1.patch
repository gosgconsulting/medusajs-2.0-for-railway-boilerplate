diff --git a/dist/customer-group-add-customers-QU4Q56SS.mjs b/dist/customer-group-add-customers-QU4Q56SS.mjs
index 890dfd735b9b0465143d1ff74835dbe1180afe2d..9439851aecff7eacc4e3694786c6390339ff7859 100644
--- a/dist/customer-group-add-customers-QU4Q56SS.mjs
+++ b/dist/customer-group-add-customers-QU4Q56SS.mjs
@@ -46,7 +46,7 @@
 
 // src/routes/customer-groups/customer-group-add-customers/components/add-customers-form/add-customers-form.tsx
 import { zodResolver } from "@hookform/resolvers/zod";
-import { Button, Checkbox, Hint, Tooltip, toast } from "@medusajs/ui";
+import { Button, Checkbox, Hint, Text, Tooltip, toast } from "@medusajs/ui";
 import {
   createColumnHelper
 } from "@tanstack/react-table";
@@ -186,47 +186,61 @@
   const { t } = useTranslation();
   const base = useCustomerTableColumns();
   const columns = useMemo(
-    () => [
-      columnHelper.display({
-        id: "select",
-        header: ({ table }) => {
-          return /* @__PURE__ */ jsx(
-            Checkbox,
-            {
-              checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
-              onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
-            }
-          );
-        },
-        cell: ({ row }) => {
-          const isPreSelected = !row.getCanSelect();
-          const isSelected = row.getIsSelected() || isPreSelected;
-          const Component = /* @__PURE__ */ jsx(
-            Checkbox,
-            {
-              checked: isSelected,
-              disabled: isPreSelected,
-              onCheckedChange: (value) => row.toggleSelected(!!value),
-              onClick: (e) => {
-                e.stopPropagation();
-              }
-            }
-          );
-          if (isPreSelected) {
+    () => {
+      const [emailCol, nameCol, ...afterName] = base;
+      return [
+        columnHelper.display({
+          id: "select",
+          header: ({ table }) => {
             return /* @__PURE__ */ jsx(
-              Tooltip,
+              Checkbox,
               {
-                content: t("customerGroups.customers.alreadyAddedTooltip"),
-                side: "right",
-                children: Component
+                checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
+                onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
               }
             );
+          },
+          cell: ({ row }) => {
+            const isPreSelected = !row.getCanSelect();
+            const isSelected = row.getIsSelected() || isPreSelected;
+            const Component = /* @__PURE__ */ jsx(
+              Checkbox,
+              {
+                checked: isSelected,
+                disabled: isPreSelected,
+                onCheckedChange: (value) => row.toggleSelected(!!value),
+                onClick: (e) => {
+                  e.stopPropagation();
+                }
+              }
+            );
+            if (isPreSelected) {
+              return /* @__PURE__ */ jsx(
+                Tooltip,
+                {
+                  content: t("customerGroups.customers.alreadyAddedTooltip"),
+                  side: "right",
+                  children: Component
+                }
+              );
+            }
+            return Component;
           }
-          return Component;
-        }
-      }),
-      ...base
-    ],
+        }),
+        emailCol,
+        nameCol,
+        columnHelper.display({
+          id: "customer_groups",
+          header: () => t("customers.fields.groups"),
+          cell: ({ row }) => {
+            const names = row.original.groups?.map((g) => g.name ?? g.id).filter(Boolean) ?? [];
+            const label = names.length ? names.join(", ") : "—";
+            return /* @__PURE__ */ jsx(Text, { size: "small", className: "text-ui-fg-subtle", children: label });
+          }
+        }),
+        ...afterName
+      ];
+    },
     [t, base]
   );
   return columns;
diff --git a/dist/customer-edit-WTBPVWOF.mjs b/dist/customer-edit-WTBPVWOF.mjs
index c9d1c577adfd628c0cb6b3e6b621b53e5f5d705b..927ad961de391e51c8e6cd1efa1e2567299b5f4f 100644
--- a/dist/customer-edit-WTBPVWOF.mjs
+++ b/dist/customer-edit-WTBPVWOF.mjs
@@ -19,6 +19,9 @@
 import "./chunk-774WSTCC.mjs";
 import "./chunk-NFEK63OE.mjs";
 import "./chunk-QZ7TP4HQ.mjs";
+import {
+  useExtension
+} from "./chunk-C5P5PL3E.mjs";
 
 // src/routes/customers/customer-edit/customer-edit.tsx
 import { Heading } from "@medusajs/ui";
@@ -42,6 +45,7 @@
 var EditCustomerForm = ({ customer }) => {
   const { t } = useTranslation();
   const { handleSuccess } = useRouteModal();
+  const { getWidgets } = useExtension();
   const form = useForm({
     defaultValues: {
       email: customer.email || "",
@@ -155,7 +159,8 @@
             ] });
           }
         }
-      )
+      ),
+      ...getWidgets("customer.edit.after").map((Widget, i) => /* @__PURE__ */ jsx(Widget, { data: customer, key: i }))
     ] }) }),
     /* @__PURE__ */ jsx(RouteDrawer.Footer, { children: /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-end gap-x-2", children: [
       /* @__PURE__ */ jsx(RouteDrawer.Close, { asChild: true, children: /* @__PURE__ */ jsx(Button, { variant: "secondary", size: "small", children: t("actions.cancel") }) }),
diff --git a/dist/customer-list-ZXCGIXJC.mjs b/dist/customer-list-ZXCGIXJC.mjs
index 2a730b3d436da16f635b4a04abc44b1ed1f125cc..4c76148747256f421d284031eb01b5bcda809548 100644
--- a/dist/customer-list-ZXCGIXJC.mjs
+++ b/dist/customer-list-ZXCGIXJC.mjs
@@ -43,7 +43,7 @@
 
 // src/routes/customers/customer-list/components/customer-list-table/customer-list-table.tsx
 import { PencilSquare } from "@medusajs/icons";
-import { Button, Container, Heading } from "@medusajs/ui";
+import { Button, Container, Heading, Text } from "@medusajs/ui";
 import { keepPreviousData } from "@tanstack/react-query";
 import { createColumnHelper } from "@tanstack/react-table";
 import { useMemo } from "react";
@@ -56,7 +56,8 @@
   const { searchParams, raw } = useCustomerTableQuery({ pageSize: PAGE_SIZE });
   const { customers, count, isLoading, isError, error } = useCustomers(
     {
-      ...searchParams
+      ...searchParams,
+      fields: "+*groups"
     },
     {
       placeholderData: keepPreviousData
@@ -130,16 +131,31 @@
 };
 var columnHelper = createColumnHelper();
 var useColumns = () => {
-  const columns = useCustomerTableColumns();
+  const { t } = useTranslation();
+  const base = useCustomerTableColumns();
   return useMemo(
-    () => [
-      ...columns,
-      columnHelper.display({
-        id: "actions",
-        cell: ({ row }) => /* @__PURE__ */ jsx(CustomerActions, { customer: row.original })
-      })
-    ],
-    [columns]
+    () => {
+      const [emailCol, nameCol, ...afterName] = base;
+      return [
+        emailCol,
+        nameCol,
+        columnHelper.display({
+          id: "customer_groups",
+          header: () => t("customers.fields.groups"),
+          cell: ({ row }) => {
+            const names = row.original.groups?.map((g) => g.name ?? g.id).filter(Boolean) ?? [];
+            const label = names.length ? names.join(", ") : "—";
+            return /* @__PURE__ */ jsx(Text, { size: "small", className: "text-ui-fg-subtle", children: label });
+          }
+        }),
+        ...afterName,
+        columnHelper.display({
+          id: "actions",
+          cell: ({ row }) => /* @__PURE__ */ jsx(CustomerActions, { customer: row.original })
+        })
+      ];
+    },
+    [t, base]
   );
 };
 
diff --git a/dist/product-detail-ATPFDPI2.mjs b/dist/product-detail-ATPFDPI2.mjs
index a2be9ca8caa13af689f54f40bcb05a257aa998d8..89558963a406df5dd97a9c02dee0613eac128e0e 100644
--- a/dist/product-detail-ATPFDPI2.mjs
+++ b/dist/product-detail-ATPFDPI2.mjs
@@ -101,6 +101,20 @@ import {
 } from "./chunk-NFEK63OE.mjs";
 import "./chunk-QZ7TP4HQ.mjs";
 
+function wcwpStripHtmlTags(raw) {
+  if (raw == null || raw === "") return "";
+  if (typeof window === "undefined") {
+    return String(raw).replace(/<[^>]*>/g, "").trim();
+  }
+  try {
+    const doc = new DOMParser().parseFromString(String(raw), "text/html");
+    const text = doc.body.textContent ?? "";
+    return text.replace(/\u00a0/g, " ").trim();
+  } catch {
+    return String(raw).replace(/<[^>]*>/g, "").trim();
+  }
+}
+
 // src/routes/products/product-detail/breadcrumb.tsx
 import { jsx } from "react/jsx-runtime";
 var ProductDetailBreadcrumb = (props) => {
@@ -118,7 +132,7 @@ var ProductDetailBreadcrumb = (props) => {
   if (!product) {
     return null;
   }
-  return /* @__PURE__ */ jsx("span", { children: product.title });
+  return /* @__PURE__ */ jsx("span", { children: wcwpStripHtmlTags(product.title) });
 };
 
 // src/routes/products/product-detail/loader.ts
@@ -221,7 +235,7 @@ var ProductGeneralSection = ({
     const res = await prompt({
       title: t("general.areYouSure"),
       description: t("products.deleteWarning", {
-        title: product.title
+        title: wcwpStripHtmlTags(product.title)
       }),
       confirmText: t("actions.delete"),
       cancelText: t("actions.cancel")
@@ -242,7 +256,7 @@ var ProductGeneralSection = ({
   };
   return /* @__PURE__ */ jsxs2(Container2, { className: "divide-y p-0", children: [
     /* @__PURE__ */ jsxs2("div", { className: "flex items-center justify-between px-6 py-4", children: [
-      /* @__PURE__ */ jsx3(Heading2, { children: product.title }),
+      /* @__PURE__ */ jsx3(Heading2, { children: wcwpStripHtmlTags(product.title) }),
       /* @__PURE__ */ jsxs2("div", { className: "flex items-center gap-x-4", children: [
         /* @__PURE__ */ jsx3(StatusBadge, { color: productStatusColor(product.status), children: t(`products.productStatus.${product.status}`) }),
         /* @__PURE__ */ jsx3(
@@ -272,8 +286,8 @@ var ProductGeneralSection = ({
         )
       ] })
     ] }),
-    /* @__PURE__ */ jsx3(SectionRow, { title: t("fields.description"), value: product.description }),
-    /* @__PURE__ */ jsx3(SectionRow, { title: t("fields.subtitle"), value: product.subtitle }),
+    /* @__PURE__ */ jsx3(SectionRow, { title: t("fields.description"), value: wcwpStripHtmlTags(product.description) }),
+    /* @__PURE__ */ jsx3(SectionRow, { title: t("fields.subtitle"), value: wcwpStripHtmlTags(product.subtitle) }),
     /* @__PURE__ */ jsx3(SectionRow, { title: t("fields.handle"), value: `/${product.handle}` }),
     /* @__PURE__ */ jsx3(SectionRow, { title: t("fields.material"), value: product.material }),
     /* @__PURE__ */ jsx3(
diff --git a/dist/product-edit-XPUNYMD7.mjs b/dist/product-edit-XPUNYMD7.mjs
index 47e953a6f71c959b81d5f20bed000238a762ca8d..016f568ddce4f3f0ad15c0c425a2c90a597262f7 100644
--- a/dist/product-edit-XPUNYMD7.mjs
+++ b/dist/product-edit-XPUNYMD7.mjs
@@ -89,6 +89,21 @@
 import { useTranslation } from "react-i18next";
 import * as zod from "zod";
 import { jsx, jsxs } from "react/jsx-runtime";
+
+function wcwpStripHtmlTags(raw) {
+  if (raw == null || raw === "") return "";
+  if (typeof window === "undefined") {
+    return String(raw).replace(/<[^>]*>/g, "").trim();
+  }
+  try {
+    const doc = new DOMParser().parseFromString(String(raw), "text/html");
+    const text = doc.body.textContent ?? "";
+    return text.replace(/\u00a0/g, " ").trim();
+  } catch {
+    return String(raw).replace(/<[^>]*>/g, "").trim();
+  }
+}
+
 var EditProductSchema = zod.object({
   status: zod.enum(["draft", "published", "proposed", "rejected"]),
   title: zod.string().min(1),
@@ -108,11 +123,11 @@
   const form = useExtendableForm({
     defaultValues: {
       status: product.status,
-      title: product.title,
+      title: wcwpStripHtmlTags(product.title),
       material: product.material || "",
-      subtitle: product.subtitle || "",
+      subtitle: wcwpStripHtmlTags(product.subtitle || ""),
       handle: product.handle || "",
-      description: product.description || "",
+      description: wcwpStripHtmlTags(product.description || ""),
       discountable: product.discountable
     },
     schema: EditProductSchema,
@@ -122,10 +137,15 @@
   const { mutateAsync, isPending } = useUpdateProduct(product.id);
   const handleSubmit = form.handleSubmit(async (data) => {
     const { title, discountable, handle, status, ...optional } = data;
-    const nullableData = transformNullableFormData(optional);
+    const strippedOptional = {
+      ...optional,
+      subtitle: optional.subtitle != null ? wcwpStripHtmlTags(String(optional.subtitle)) : optional.subtitle,
+      description: optional.description != null ? wcwpStripHtmlTags(String(optional.description)) : optional.description
+    };
+    const nullableData = transformNullableFormData(strippedOptional);
     await mutateAsync(
       {
-        title,
+        title: wcwpStripHtmlTags(title),
         discountable,
         handle,
         status,
diff --git a/dist/chunk-BYOPZAGX.mjs b/dist/chunk-BYOPZAGX.mjs
index cebcbc2878bcff4ac9c4bcfc91a1623ce68b3a8f..e436c3ab88163da9bdb4a54231b5464de2afb250 100644
--- a/dist/chunk-BYOPZAGX.mjs
+++ b/dist/chunk-BYOPZAGX.mjs
@@ -3,7 +3,7 @@
 import { Text, clx } from "@medusajs/ui";
 import { useRef, useState } from "react";
 import { jsx, jsxs } from "react/jsx-runtime";
-var DEFAULT_MAX_FILE_SIZE = 1024 * 1024;
+var DEFAULT_MAX_FILE_SIZE = 10 * 1024 * 1024;
 var FileUpload = ({
   label,
   hint,
diff --git a/dist/chunk-DODQ3KJT.mjs b/dist/chunk-DODQ3KJT.mjs
index 068317b48d9e15296e6e1931e180a912cf03f8c6..e6c9f0db57810311cca5184e7a191f98c0af9109 100644
--- a/dist/chunk-DODQ3KJT.mjs
+++ b/dist/chunk-DODQ3KJT.mjs
@@ -288,7 +288,7 @@
           type: "file_too_large",
           message: t("products.media.fileTooLarge", {
             name: fileNames,
-            size: "1MB"
+            size: "10MB"
           })
         });
         hasInvalidFile = true;
diff --git a/dist/app.js b/dist/app.js
index a811fb3933518fd9dac5310a9041bea1ef05e688..c8c8ad27f97e9b0521e8ea79ebfcf42fd27b26f1 100644
--- a/dist/app.js
+++ b/dist/app.js
@@ -106865,7 +106865,7 @@
     import_ui55 = require("@medusajs/ui");
     import_react71 = require("react");
     import_jsx_runtime77 = require("react/jsx-runtime");
-    DEFAULT_MAX_FILE_SIZE = 1024 * 1024;
+    DEFAULT_MAX_FILE_SIZE = 10 * 1024 * 1024;
     FileUpload = ({
       label,
       hint,
@@ -107046,7 +107046,7 @@
               type: "file_too_large",
               message: t5("products.media.fileTooLarge", {
                 name: fileNames,
-                size: "1MB"
+                size: "10MB"
               })
             });
             hasInvalidFile = true;
