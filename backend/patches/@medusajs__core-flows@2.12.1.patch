diff --git a/dist/cart/workflows/get-variants-and-items-with-prices.js b/dist/cart/workflows/get-variants-and-items-with-prices.js
--- a/dist/cart/workflows/get-variants-and-items-with-prices.js
+++ b/dist/cart/workflows/get-variants-and-items-with-prices.js
@@ -7,6 +7,49 @@
 const steps_1 = require("../steps");
 const fields_1 = require("../utils/fields");
 const prepare_line_item_data_1 = require("../utils/prepare-line-item-data");
+function wcwpNormalizeGroupName(name) {
+    return String(name ?? "").toLowerCase().replace(/[\s-]+/g, "_");
+}
+function wcwpMetaKeyForCustomerGroups(groups) {
+    if (!groups?.length) {
+        return null;
+    }
+    for (const g of groups) {
+        const n = wcwpNormalizeGroupName(g?.name);
+        if (n === "client_a" || n === "clienta" || n.endsWith("_client_a")) {
+            return "wcwp_client-a";
+        }
+        if (n === "client_b" || n === "clientb" || n.endsWith("_client_b")) {
+            return "wcwp_client-b";
+        }
+        if (n === "client_c" || n === "clientc" || n.endsWith("_client_c")) {
+            return "wcwp_client-c";
+        }
+    }
+    return null;
+}
+function wcwpMajorToMinor(major, currencyCode) {
+    const c = String(currencyCode ?? "usd").toLowerCase();
+    if (c === "jpy" || c === "krw" || c === "vnd" || c === "clp" || c === "uyw") {
+        return Math.round(major);
+    }
+    return Math.round(major * 100);
+}
+function wcwpParseMajorPrice(raw) {
+    if (raw == null || raw === "") {
+        return null;
+    }
+    const n = Number(String(raw).trim().replace(",", "."));
+    return Number.isFinite(n) && n >= 0 ? n : null;
+}
+function wcwpFallbackCurrencyCode() {
+    const raw = process.env.WCWP_FALLBACK_CURRENCY;
+    if (raw == null || String(raw).trim() === "") {
+        return "eur";
+    }
+    return String(raw).trim().toLowerCase();
+}
+const WCWP_FALLBACK_CURRENCY = wcwpFallbackCurrencyCode();
 exports.getVariantsAndItemsWithPricesId = "get-variant-items-with-prices-workflow";
 exports.getVariantsAndItemsWithPrices = (0, workflows_sdk_1.createWorkflow)(exports.getVariantsAndItemsWithPricesId, (input) => {
     const variantIds = (0, workflows_sdk_1.transform)({ cart: input.cart, items: input.items, variantIds: input.variants?.id }, (data) => {
@@ -21,12 +64,13 @@
         setPricingContextResult: input.setPricingContextResult,
     }, (data) => {
         const cart = data.cart;
+        const rawCurrency = cart.currency_code ?? cart.region?.currency_code;
         const baseContext = {
             ...(0, utils_1.filterObjectByKeys)(cart, fields_1.cartFieldsForPricingContext),
             customer: cart.customer,
             region: cart.region,
             ...(data.setPricingContextResult ? data.setPricingContextResult : {}),
-            currency_code: cart.currency_code ?? cart.region?.currency_code,
+            currency_code: typeof rawCurrency === "string" ? rawCurrency.toLowerCase() : rawCurrency,
             region_id: cart.region_id,
             customer_id: cart.customer_id,
         };
@@ -61,26 +105,56 @@
     }).config({ name: "fetch-variants" });
     const calculatedPriceSets = (0, steps_1.getVariantPriceSetsStep)({
         data: cartPricingContext,
+    }).config({ name: "wcwp-get-variant-price-sets-primary" });
+    const eurFallbackPricingContext = (0, workflows_sdk_1.transform)({ cartPricingContext, cart: input.cart }, ({ cartPricingContext: ctxRows, cart, }) => {
+        const rawCurrency = String(cart?.currency_code ?? cart?.region?.currency_code ?? "").toLowerCase();
+        if (rawCurrency === WCWP_FALLBACK_CURRENCY || !ctxRows?.length) {
+            return [];
+        }
+        return ctxRows.map((entry) => ({
+            ...entry,
+            context: {
+                ...entry.context,
+                currency_code: WCWP_FALLBACK_CURRENCY,
+            },
+        }));
     });
+    const eurCalculatedPriceSets = (0, steps_1.getVariantPriceSetsStep)({
+        data: eurFallbackPricingContext,
+    }).config({ name: "wcwp-get-variant-price-sets-fallback" });
     const variantsItemsWithPrices = (0, workflows_sdk_1.transform)({
         cart: input.cart,
         items: input.items,
         variantsData,
         calculatedPriceSets,
-    }, ({ cart, items: inputItems, variantsData, calculatedPriceSets, }) => {
+        eurCalculatedPriceSets,
+    }, ({ cart, items: inputItems, variantsData, calculatedPriceSets, eurCalculatedPriceSets, }) => {
         const priceNotFound = [];
         const variantNotFoundOrPublished = [];
         const items = (inputItems ?? cart.items ?? []).map((item) => {
             const item_ = item;
+            const variant = variantsData.find((v) => v.id === item.variant_id);
+            const wcwpTierKey = wcwpMetaKeyForCustomerGroups(cart?.customer?.groups);
+            const wcwpAllowTier = !(0, utils_1.isDefined)(item?.unit_price) && !item_?.is_custom_price;
+            const wcwpTierMajor = wcwpTierKey && variant && wcwpAllowTier
+                ? wcwpParseMajorPrice(variant.metadata?.[wcwpTierKey])
+                : null;
+            const wcwpHasTierPrice = wcwpTierMajor != null;
             const idLike = item.id ?? (0, utils_1.simpleHash)(JSON.stringify(item));
             let calculatedPriceSet = calculatedPriceSets[idLike];
             if (!calculatedPriceSet) {
                 calculatedPriceSet = calculatedPriceSets[item_.variant_id];
             }
-            if (!calculatedPriceSet && item_.variant_id) {
+            if (!calculatedPriceSet) {
+                calculatedPriceSet = eurCalculatedPriceSets[idLike];
+            }
+            if (!calculatedPriceSet) {
+                calculatedPriceSet = eurCalculatedPriceSets[item_.variant_id];
+            }
+            const isCustomPrice = (item_.is_custom_price ?? (0, utils_1.isDefined)(item?.unit_price)) || wcwpHasTierPrice;
+            if (!calculatedPriceSet && item_.variant_id && !isCustomPrice) {
                 priceNotFound.push(item_.variant_id);
             }
-            const variant = variantsData.find((v) => v.id === item.variant_id);
             if ((item.variant_id && !variant) || // variant specified but doesn't exist
                 (variant && (!variant?.product?.status || variant.product.status !== utils_1.ProductStatus.PUBLISHED)) // variant exists but product is not published
             ) {
@@ -89,7 +163,6 @@
             if (variant) {
                 variant.calculated_price = calculatedPriceSet;
             }
-            const isCustomPrice = item_.is_custom_price ?? (0, utils_1.isDefined)(item?.unit_price);
             const input = {
                 item: item_,
                 variant: variant,
@@ -99,11 +172,17 @@
                     calculatedPriceSet?.is_calculated_price_tax_inclusive,
                 isCustomPrice: isCustomPrice,
             };
-            if (variant && !isCustomPrice) {
+            if (variant && !isCustomPrice && calculatedPriceSet) {
                 input.unitPrice = calculatedPriceSet.calculated_amount;
                 input.isTaxInclusive =
                     calculatedPriceSet.is_calculated_price_tax_inclusive;
             }
+            if (variant && wcwpHasTierPrice) {
+                const cur = cart.currency_code ?? cart.region?.currency_code;
+                input.unitPrice = wcwpMajorToMinor(wcwpTierMajor, cur);
+                input.isCustomPrice = true;
+                input.isTaxInclusive = false;
+            }
             const preparedItem = (0, prepare_line_item_data_1.prepareLineItemData)(input);
             return {
                 selector: { id: item_.id },
@@ -120,4 +199,4 @@
     });
     return new workflows_sdk_1.WorkflowResponse(variantsItemsWithPrices);
 });
-//# sourceMappingURL=get-variants-and-items-with-prices.js.map
\ No newline at end of file
+//# sourceMappingURL=get-variants-and-items-with-prices.js.map
diff --git a/dist/cart/utils/fields.js b/dist/cart/utils/fields.js
--- a/dist/cart/utils/fields.js
+++ b/dist/cart/utils/fields.js
@@ -135,6 +135,7 @@
     "sales_channel_id",
     "currency_code",
     "region_id",
+    "region.currency_code",
     "shipping_address.city",
     "shipping_address.country_code",
     "shipping_address.province",
@@ -144,9 +145,11 @@
     "customer.id",
     "email",
     "customer.groups.id",
+    "customer.groups.name",
 ];
 exports.productVariantsFields = [
     "id",
+    "metadata",
     "title",
     "sku",
     "manage_inventory",
diff --git a/dist/cart/workflows/refresh-cart-shipping-methods.js b/dist/cart/workflows/refresh-cart-shipping-methods.js
--- a/dist/cart/workflows/refresh-cart-shipping-methods.js
+++ b/dist/cart/workflows/refresh-cart-shipping-methods.js
@@ -109,7 +109,7 @@
                 const shippingOptionPrice = shippingOption?.calculated_price?.calculated_amount;
                 // The shipping method is only valid if both the shipping option and the price is found
                 // for the context of the cart. The invalid options will lead to a deleted shipping method
-                if ((0, utils_1.isPresent)(shippingOption) && (0, utils_1.isDefined)(shippingOptionPrice)) {
+                if ((0, utils_1.isPresent)(shippingOption) && (0, utils_1.isPresent)(shippingOptionPrice)) {
                     return true;
                 }
                 return false;
@@ -117,16 +117,22 @@
             const shippingMethodIds = shippingMethods.map((sm) => sm.id);
             const validShippingMethodIds = validShippingMethods.map((sm) => sm.id);
             const invalidShippingMethodIds = shippingMethodIds.filter((id) => !validShippingMethodIds.includes(id));
-            const shippingMethodsToUpdate = validShippingMethods.map((shippingMethod) => {
+            const shippingMethodsToUpdate = validShippingMethods
+                .map((shippingMethod) => {
                 const shippingOption = shippingOptions.find((s) => s.id === shippingMethod.shipping_option_id);
+                const cp = shippingOption?.calculated_price;
+                const amount = cp?.calculated_amount;
+                if (!(0, utils_1.isPresent)(shippingOption) || !(0, utils_1.isPresent)(amount)) {
+                    return null;
+                }
                 return {
                     id: shippingMethod.id,
                     shipping_option_id: shippingOption.id,
-                    amount: shippingOption.calculated_price.calculated_amount,
-                    is_tax_inclusive: shippingOption.calculated_price
-                        .is_calculated_price_tax_inclusive,
+                    amount,
+                    is_tax_inclusive: !!cp.is_calculated_price_tax_inclusive,
                 };
-            });
+            })
+                .filter(Boolean);
             return {
                 shippingMethodsToRemove: invalidShippingMethodIds,
                 shippingMethodsToUpdate,
